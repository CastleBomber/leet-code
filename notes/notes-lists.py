"""
PYTHON LIST METHODS REFERENCE
---------------------------------------------------------------------------
Method           | Action                     | Example (nums = [1, 2])
---------------------------------------------------------------------------
.append(x)       | Adds x to the end          | [1, 2, 3]
.extend(iterable)| Appends multiple items     | [1, 2, 4, 5, 6]
.insert(i, x)    | Inserts x at index i       | [1, x, 2]
.pop(i)          | Removes/returns item at i  | nums.pop(0) -> 1
.remove(x)       | Removes first occurance x  | nums.remove(2)
.sort()          | Sorts list in-place (O(nlogn))| nums.sort()
.reverse()       | Reverses list in-place     | [2, 1]
.index(x)        | Returns index of first x   | nums.index(2) -> 1
.count(x)        | Returns number of x's      | nums.count(1) -> 1
.clear()         | Removes all elements       | []
---------------------------------------------------------------------------
"""